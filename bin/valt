#!/usr/bin/env rayvn-bash
# shellcheck disable=SC2155

usage() {
    echo
    show bold "Public key encryption of files and directories, using" cyan "age" "(see" blue "${ageProjectUrl}" glue ")."
    echo
    show "Usage:" bold blue "${scriptName}" "[OPTIONS]" italic "COMMAND"
    echo
    echo "Commands"
    echo
    echo "    pass             Generate and/or evaluate password/passphrase strength."
    echo "    keys             Generate a key pair with a password/passphrase encrypted private key."
    echo "    archive          Create a secure archive from one or more files or directories, or extract files from one."
    echo
    echo "Options"
    echo
    commonOptions 21
    echo
    echo "Environment Variables"
    echo
    echo "    VALT_TIMEOUT     Set input timeout, in seconds (default: 30)."
    echo
    show "Run" bold blue "${projectName}" italic "COMMAND" bold blue "--help" "for more information on a command."
    echo
    bye "${@}"
}

main() {
    init "${@}"
    if (( newPassphrase )); then
        require 'valt/password'
        generatePassphrase
    elif [[ -n ${command} ]]; then
        executeClean ${command} "${commandOptions[@]}"  # TODO: revisit need for clean vars!
    else
        usage
    fi
}

init() {

    # Global constants

    declare -grx ageProjectUrl='https://github.com/filosottile/age'
    declare -grx valtBinDir="${0%/*}"
    declare -grx valtConfigDir="${ configDirPath; }"
    declare -grx valtProjectUrl='https://github.com/phoggy/valt'

    # Global options

    declare -gx passwordVisibility="${VALT_VISIBILITY:-none}"           # none|hide|show
    declare -gx expertMode="${VALT_EXPERT_MODE:-}"                      # any value enables
    declare -grx inputTimeout="${VALT_INPUT_TIMEOUT:-30}"               # seconds

    # Arguments

    declare -g newPassphrase=0
    declare -g command
    declare -ga commandOptions=()

    # Parse input

    [[ $1 ]] || usage

    while (( $# > 0 )); do
        case "${1}" in
            pass) newPassphrase=1 ;;
            keys) setCommand keys ;;
            encrypt) setCommand encrypt ;;
            decrypt) setCommand decrypt ;;
            --expert) expertMode=true ;; # TODO update usage when implemented
            --visibility) shift; passwordVisibility="${1}" ;;
            -h | --help) [[ -n ${command} ]] && commandOptions+=("${1}") || usage ;;
            -v) projectVersion valt; exit 0 ;;
            --version) projectVersion valt true; exit 0 ;;
            --debug*) setDebug "$@"; shift $? ;;
            *) commandOptions+=("${1}") ;;
        esac
        shift
    done

    #    debugVar passwordVisibility expertMode proxyPinEntry command commandOptions

    assertValidVisibility
    [[ ${newPassphrase} || -n ${command} ]] || usage "missing command"

    checkProjectDependencies 'valt'
}

setCommand() {
    command="${valtBinDir}/${1}"
}

assertValidVisibility() {
    case "${passwordVisibility}" in
        none | hide | show) ;;
        *) usage "unknown visibility mode: '${passwordVisibility}'"
    esac
}

# Boot rayvn with initial required libraries

source rayvn.up 'rayvn/core' 'rayvn/dependencies' || exit

main "${@}"



